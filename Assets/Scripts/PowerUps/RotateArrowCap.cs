using UnityEngine;

public class RotateArrowCap : MonoBehaviour
{
    void Update()
    {
	    //if (!PowerUpsManager.Instance)
		//    return;
	    
	    /*Vector3 capPos = PowerUpsManager.Instance.GetCapPosition ();
    
	    Vector3 dirCap = capPos - this.transform.position;
	    dirCap.Normalize ();

	    this.gameObject.transform.LookAt (capPos, new Vector3(0,1,0));
	    this.gameObject.transform.position = new Vector3(this.transform.position.x + dirCap.x * 2, 5, this.transform.position.z + dirCap.z * 2);*/
    }
}
